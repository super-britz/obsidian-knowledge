---
type: 章节笔记
status: 已整理
chapter: 5
created: 2026-08-11
updated: 2026-08-12
sources:
  - "raw/links/2026-08-11-Transformer架构从直觉到实现.md"
---

# 位置编码：模型怎么知道 Token 的先后顺序

> [!abstract] Embedding 告诉模型“这是什么 Token”，位置编码补充“它排在第几位”。两者合在一起，模型才有机会区分“我爱你”和“你爱我”。

## 先看流程

```text
Token 的 Embedding：内容是什么
+
位置向量：它排在哪里
=
带有内容和顺序信息的输入
→ 后面的 Attention 再利用前后和距离理解句子
```

两个相同 Token 的初始 Embedding 相同；加上不同位置向量后，它们进入 Transformer 的表示才不同。

## 关键理解

- **Transformer 不会天然知道顺序。** 它不像 RNN 那样按顺序传递状态，所以需要主动加入位置信号。
- **原始 Transformer 用 sin / cos 生成位置向量。** 可以把它理解成多组快慢不同的“时钟”，组合后为每个位置提供不同指纹。
- **位置编码只提供线索，不独自理解语义。** 后面的 Attention 和网络层还要结合内容、先后和距离，才能理解语法与指代。
- **位置方案不止一种。** 固定正弦编码、可学习位置向量、RoPE、ALiBi 都在解决顺序问题；能计算更远位置不保证模型一定能稳定理解超长文本。

## 做项目时记住

1. **上下文不是无序文件夹。** 系统规则、当前任务、可靠资料和工具结果的排列会影响模型怎样使用它们。
2. **关键内容要有清晰层次。** 用标题、字段和短摘要让模型更容易找到当前目标，而不是用一大段混杂文本。
3. **多步 Agent 要按阶段重建上下文。** 不要无限追加历史；每个阶段重新选择真正需要的信息。

## 别误解

- **“Token 本身就带顺序。”** Token ID 只说明是什么片段，不说明它在第几位。
- **“位置编码本身懂语法。”** 它只标记位置，语义关系要由后续网络学习。
- **“支持长窗口就不必检索和摘要。”** 长上下文仍会受预算、模型训练和信息组织质量限制。

## 复习

1. 为什么“我爱你”和“你爱我”需要位置编码才能在输入阶段区分？
2. 为什么相同检索资料换一种顺序，模型回答可能变化？
3. 为什么多步 Agent 不应只不断追加聊天历史？

## 来源与下一步

- **来源事实：** 第 5 章说明 Embedding 本身不带位置；原始 Transformer 用 sin / cos 生成位置向量，并与 Embedding 相加后送入 Transformer Block。
- **本页推论：** Prompt、检索资料和 Agent 状态需要按优先级和阅读顺序编排。
- **下一步：** [[06：LayerNorm 与 Softmax|LayerNorm 与 Softmax]]、Attention 与 QKV（待整理）。
- **来源：** [[raw/links/2026-08-11-Transformer架构从直觉到实现|Transformer 架构：从直觉到实现]]。
